name: EAS Build & Preview

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: EAS Build
    runs-on: ubuntu-latest

    # 掛在 job 層級，所有 step 都能讀到
    env:
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Setup EAS CLI
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      # PR → preview build，加 --no-wait 讓 CI 馬上回傳（不等 build 跑完）
      - name: EAS Preview Build (PR)
        if: github.event_name == 'pull_request'
        run: eas build --platform all --non-interactive --profile preview --no-wait

      # push to main → production build (Android only until Apple Developer account is set up)
      - name: EAS Production Build (main)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: eas build --platform android --non-interactive --profile production --no-wait
